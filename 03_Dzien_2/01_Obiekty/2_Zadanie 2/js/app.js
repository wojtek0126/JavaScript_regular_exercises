const timeMachine = {
    shape: 'shapeValue',
    model: 'modelValue',
    run: function(date, place) {
        console.log('date:', date, 'place:', place);
    }
};


timeMachine.run('30.08.2020', 'Sopot');